package com.example.socialdeal.data.values

enum class ErrorTypes {
    FORBIDDEN,
    UNAUTHORISED,
    BAD_REQUEST,
    NOT_FOUND,
    JOB_CANCELLED,
    NETWORK_ERROR,
    NETWORK_TIMEOUT,
    UNKNOWN
}